<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.react.repository.MemberRepository">

    <select id="joinMembers" parameterType="com.example.react.Dtos.MembersDto" resultType="int">
        insert into members (username, join_date, naverEmail, phoneNum, phone_auth_yn) values (#{username}, now(), #{naverEmail}, #{phoneNum}, 1);
    </select>

</mapper>